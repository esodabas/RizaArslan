{
  "name": "riza-arslan-web",
  "version": "1.0.0",
  "description": "Root package for Railway deployment",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "node backend/server.js"
  }
}
